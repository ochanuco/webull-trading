{
  "name": "webull-trading",
  "private": true,
  "version": "1.0.2",
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "tsx scripts/guard-deploy.ts && wrangler deploy",
    "deploy:dev": "wrangler d1 migrations apply webull-trading-dev --env=dev --remote && wrangler deploy --env=dev",
    "deploy:staging": "wrangler d1 migrations apply webull-trading-staging --env=staging --remote && wrangler deploy --env=staging",
    "deploy:production": "pnpm verify:production-d1 && wrangler d1 migrations apply webull-trading-production --env=production --remote && wrangler deploy --env=production",
    "deploy:production:dry-run": "pnpm verify:production-d1 && wrangler deploy --env=production --dry-run",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "accounts": "tsx scripts/list-webull-accounts.ts",
    "issue-token": "tsx scripts/issue-webull-token.ts",
    "verify:production-d1": "tsx scripts/verify-production-d1.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.14",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.20.6",
    "typescript": "^7.0.0",
    "vite": "^8.0.14",
    "vitest": "^4.0.0",
    "wrangler": "^4.0.0"
  }
}
